{
  "name": "react-lib-pnpm-storybook",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": "true",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "lerna": "^6.6.2"
  },
  "workspaces": [
    "packages/*",
    "app/*"
  ]
}

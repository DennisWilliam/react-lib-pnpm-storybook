import React from 'react'
import BaseButton from '../../base'
import { CircleButtonProps } from '../../base/buttonTypes'

const CircleButton: React.FC<CircleButtonProps> = (buttonProps) => <BaseButton {...buttonProps} />

export default CircleButton

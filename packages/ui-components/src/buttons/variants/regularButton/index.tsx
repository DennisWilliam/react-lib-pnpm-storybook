import React from 'react'
import BaseButton from '../../base'
import { RegularButtonProps } from '../../base/buttonTypes'

const RegularButton: React.FC<RegularButtonProps> = (buttonProps) => <BaseButton {...buttonProps} />

export default RegularButton
